{
  "name": "safegamingos",
  "private": true,
  "version": "0.9.1",
  "type": "module",
  "description": "safegamingOS — Qubes-like room isolation on Arch Linux for gaming and streaming (archiso build project, no web app)",
  "scripts": {
    "typecheck": "tsc -b --noEmit",
    "preview": "node scripts/serve-preview.mjs",
    "build": "node scripts/build-preview.mjs",
    "gen:assets": "node scripts/gen-assets.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
